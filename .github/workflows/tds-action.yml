name: TDS Action

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 23f2004044@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done
        run: echo "Finished"
